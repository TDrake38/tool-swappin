import React from "react";
import { Button } from "react-bootstrap";

function SaveButton() {
  return (
    <Button variant="dark" className="bootan">
      Save
    </Button>
  );
}

export default SaveButton;
