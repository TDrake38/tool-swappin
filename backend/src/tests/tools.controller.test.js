const tool = require('../controllers/tools.controller')

// trying to test the first findTools controller

// test('the data is all tools', done => {
//     function findTools(error, data) {
//       if (error) {
//         done(error);
//         return;
//       }
//       try {
//         expect(data).toBe('hammer');
//         done();
//       } catch (error) {
//         done(error);
//       }
//     }
  
//     fetchData(findTools);
//   });